let request = require('request');
exports.lambda_handler = function(event, context, callback){
  // This needs to handle the basic routing of our various 
  // storage types.  This one is just the router, the real logic
  // lies in the specific type handlers.
}

